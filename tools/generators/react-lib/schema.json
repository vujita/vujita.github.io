{
  "cli": "nx",
  "id": "react-lib",
  "properties": {
    "name": {
      "type": "string",
      "description": "Library name",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What would you like to name the library?"
    }
  },
  "required": ["name"],
  "type": "object"
}
