{
  "description": "Generate typescript definitions (*.d.ts) files for CSS modules",
  "properties": {
    "globPattern": {
      "type": "string",
      "description": "The glob pattern to find scss files",
      "default": "src"
    },
    "includePaths": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [],
      "description": "An array of paths to look in to attempt to resolve @import declarations"
    },
    "watch": {
      "type": "boolean",
      "default": false,
      "description": "Watch for files that get added or changed"
    }
  },
  "title": "Typed Sass modules builder",
  "type": "object"
}
